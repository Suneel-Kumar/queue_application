import React from 'react';
import { getCompanies } from '../config/config';

const firebaseData = () => {

    const getAllCompanies = async () => {
        
    }
    return (
        <div>
            
        </div>
    )
}

export default firebaseData;

//    Search Two Types
// 1. full text search
// 2. Elastic search

// cron jobs


